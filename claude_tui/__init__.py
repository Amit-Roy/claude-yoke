"""claude_tui — a terminal UI for Claude Code.

A multi-pane TUI front-end that drives the real ``claude`` CLI as its engine:

* an extensible activity bar (left pane of buttons),
* a switchable sidebar (chat sessions browser / ``.claude`` file tree),
* a live token-estimate panel and a running-agents panel,
* a chat transcript + composer occupying the right half of the screen.
"""

__version__ = "0.1.0"
